# macOS system files
.DS_Store
*/.DS_Store
**/.DS_Store

# Local-only raw / scratch files — never upload (module authoring: OCR, batches, scripts)
no_upload/

# Local-only Claude guidance — keep local, do not publish to the repo
CLAUDE.md
.claude/
